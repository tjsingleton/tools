# Handoff — 2026-04-30 (Package A complete; Packages 0/B/C/D remain)

## Where we are

Approved plan: `.omc/plans/drop-polished-add-fingerprint.md` (DELIBERATE; PASS+APPROVE consensus). Reviews at `.architect.md` and `.critic.md`. Spike artifacts in `.omc/spikes/`.

**Package A — Audio fingerprinting: SHIPPED.**
- Commit `27bee9c` on branch `mvp-vertical`.
- 81 `AudioFingerprinted` events landed in `~/Library/KnowledgePipeline/events.db`.
- Real-corpus cluster report locked in: **14 duplicate clusters in 81 entries → 66 unique recordings.** 12/14 are renamed-`.m4a` ↔ timestamped-`.m4a` pairs from re-export-after-rename. One triple (`AirPods` ×3). One `.qta` pair (`Hidden Shoals Rd 3 / 3 2`). Full report at `.omc/spikes/fingerprint-clusters-2026-04-30.json`.
- Tests: 52/52 passing (golden-fixture failure pre-existing and unrelated).
- Architect O1 (per-file FingerprintFailed → skip-not-halt) and O2 (per-run `seen_fp` doesn't dedupe against persisted store) honored in code; O2 surfaces post-hoc via the cluster report.

## What's left

| Package | Scope | Pre-condition |
|---|---|---|
| **0** — Corpus completeness ingest | Discover & ingest the ~50 audio files on disk that aren't in events.db; full pipeline (transcribe→diarize→analyze→embed); fingerprint dedup is now live so `.qta`/`.m4a` re-encode pairs collapse at discovery for new files. | None — Package A done; embed-backfill predicate already satisfied. |
| **B** — Polished → reference migration | Disable `polished_memo` plugin in `cli._get_plugin`; new `ReferenceTranscriptAttached` + `ReferenceTranscriptDetached` events; basename-match attach (recall now ~90%+ post-Package-0); centralized `_should_hide_polished_orphan` UI helper; `kp events tail --source polished_memo` diagnostic CLI. | Package 0 complete (orphan rate must drop from 36.4% to near-zero). |
| **C** — Tests & verification | Unit + integration + E2E + observability per the expanded test plan; manual UI walkthrough; observability dashboards/log assertions. | Packages A+0+B complete. |
| **D** — Diarization performance (follow-up) | D1 MPS, D2 silence-trim (new `ItemPreprocessed` event), D3 VAD-gated single-speaker skip. Out of scope for the primary plan but already authored as Follow-ups. | Independent. Highest leverage to ship next. |

Plus several **Follow-ups** from the ADR (each is a separate small effort): `MemosMerged` curation event + UI to consolidate the 14 known clusters; `ItemSuperseded` migration; WhisperX-accuracy evaluator consuming `ReferenceTranscriptAttached`; `DeadLetterEmbed` event for persistent embed failures; AssemblyAI/Deepgram backend (~$5–6 for full corpus, optional D4).

## Operational state to preserve

- Branch: `mvp-vertical`. Working tree is clean except for pre-existing `.DS_Store`, `.claude/settings.local.json`, `.claude/skills/openrouter-usage/`.
- Events DB: 81 voice_memo `ItemIngested`, 81 `AudioFingerprinted`, 80 `EmbeddingCompleted` (1 empty-transcript skip on `20260407 093418-16CD3C80.m4a`), 66 polished_memo `ItemIngested` (24 will pair to voice_memo on Package B; 42 currently orphan because their audio is missing — Package 0 fixes this).
- `DEFAULT_STAGES = "ingest,normalize,transcribe,diarize,analyze"` — embed and curate require explicit `--stages` opt-in.
- Diarization is CPU-pinned (`whisperx_backend.py:28 device="cpu"`); long recordings dominate runtime. Embed backfill took ~70 min on the 81 items.
- The 50-ish files on disk not yet in events.db are the bulk of remaining ingest work for Package 0.

## How to resume

Use the `/team` skill to preserve context window in subsequent sessions. The prompt below is self-contained — paste it into a fresh Claude Code session in this repo.

### Resumption prompt for `/team`

```
/oh-my-claudecode:team 4 Resume the knowledge-pipeline plan at
.omc/plans/drop-polished-add-fingerprint.md after Package A
(commit 27bee9c) by executing Package 0 — corpus completeness
ingest pass. Plan is APPROVED (Architect PASS, Critic APPROVE).

Repo: /Volumes/DataDock/Users/tjsingleton/src/github.com/tjsingleton/tools/tools/knowledge-pipeline
Branch: mvp-vertical

Read first:
- HANDOFF-2026-04-30.md (this session's status)
- .omc/plans/drop-polished-add-fingerprint.md (full plan; §Package 0)
- .omc/plans/drop-polished-add-fingerprint.architect.md observations O1 + O2
- .omc/spikes/fingerprint-clusters-2026-04-30.json (the 14 known
  duplicate clusters — these are pre-existing in the DB and will
  surface again as expected; do NOT auto-merge them)

Package 0 work breakdown for the team:

Worker A (audit + run): Write scripts/audit_dump_dir.py per §0.1 —
list audio files in /Volumes/DataDock/Users/tjsingleton/Archives/Misc/VOICE\
 MEMO\ DUMP whose sha256(file_bytes) is NOT already in events.db
ItemIngested. Persist gap to .omc/spikes/dump-dir-gap-<date>.json.
Then run `kp run --source voice_memo --path "..." --stages
transcribe,diarize,analyze,embed --no-dry-run`. Watch for §0.3
halt-and-reassess gates (90-min single-transcribe; >2 consecutive
silent embed_stage skips).

Worker B (verify + report): While ingest runs, monitor events.db
deltas every 10 minutes. Track fingerprint cluster growth — if any
new ingest collapses with an existing entry via the now-live
fingerprint dedup, log it. After Worker A's ingest completes:
verify the embed-backfill predicate from .omc/spikes/embed-backfill-criterion.md
returns 0 rows scoped to the new cohort; verify every newly-ingested
item has a sibling AudioFingerprinted event; produce a Package 0
completion report at .omc/spikes/package-0-complete-<date>.md.

Worker C (Package D1 in parallel): Implement the D1 MPS-for-diarize
fix (`whisperx_backend.py`) — change device default to "auto",
prefer mps on Apple Silicon, fall back to cpu on incompatible ops.
Acceptance: a fixture diarization completes ≥3× faster than CPU
baseline. THIS IS INDEPENDENT — do not block on Workers A/B. Land
behind a small unit test.

Worker D (audit ingest parallelism feasibility): Measure whether
sharding the dump dir into 2-3 parallel `kp run` invocations
on the same events.db is a free win. Concerns: pyannote model
double-load (~2GB each), CPU contention, SQLite WAL behavior under
N writers. Produce .omc/spikes/parallelism-feasibility.md with
verdict: USE_PARALLEL/SINGLE_PROCESS/MPS_FIRST. Recommend the right
shape for the actual ingest run BEFORE Worker A starts the long-
running ingest. Worker A waits for Worker D's verdict.

Use git worktrees if package-D and package-0 need to touch shared
files; otherwise run in-tree. Commit each worker's output as a
separate commit referencing this handoff. Cancel curate stage
explicitly — user does not want auto-publish.

Constraints:
- Do not run `--stages curate` anywhere
- Do not auto-merge the 14 known fingerprint clusters
- Do not touch ~/Projects/pkb/
- Use /team workers (not direct Agent calls) so context stays small
- One commit per package; reference HANDOFF-2026-04-30.md in commit body
```

### After Package 0 ships

The follow-up team prompt for **Package B** (polished → reference migration) will need to be drafted then. Pre-condition: Package 0 confirmed complete and basename-match recall measured against the now-fuller corpus (re-run the basename-match dry-run; expect ~90%+ recall vs the prior 36.4%).

## Open questions

- **Should we run the existing 14 fingerprint clusters through a `MemosMerged` curation pass before Package 0?** Pro: cleaner DB before adding more entries. Con: requires designing the curation event + UI, which the plan defers. Default: leave as-is; the cluster report is the audit trail; merging is a separate later effort.
- **Package D1 (MPS for diarize) sequencing**: drop-in fix that makes Package 0 ingest 3-5× faster. Recommend doing D1 BEFORE Package 0, not after, despite its "follow-up" classification. The team prompt above assumes parallel; if D1 lands first, Package 0 ingest finishes much sooner.
- **AssemblyAI backend (D4)**: ~$5-6 for a one-time corpus pass (vs ~$0 local but multi-hour). Skip unless local diarization remains painful after D1+D2+D3.

## Files of record (this session)

- `.omc/plans/drop-polished-add-fingerprint.md` (final plan, deliberate mode)
- `.omc/plans/drop-polished-add-fingerprint.architect.md` (PASS)
- `.omc/plans/drop-polished-add-fingerprint.critic.md` (APPROVE)
- `.omc/spikes/pcm-hash-{verdict.md,results.json}` — PCM_INSUFFICIENT
- `.omc/spikes/basename-match-{verdict.md,results.json}` — BASENAME_POOR (corpus gap)
- `.omc/spikes/embed-backfill-{criterion.md,status.md}` — predicate
- `.omc/spikes/fingerprint-clusters-2026-04-30.json` — 14 clusters
- This file: `HANDOFF-2026-04-30.md`
